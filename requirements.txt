fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pytest==8.3.3

# Optional: only needed if you set ANTHROPIC_API_KEY to enable AI-sharpened
# tips, recaps, chat, budget alerts, and smart expense parsing — the app runs
# fine without it via rule-based fallbacks (see app/ai.py)
anthropic==0.39.0
